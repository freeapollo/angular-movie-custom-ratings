import angular from 'angular';

import ratingBarChartComponent from './rating-bar-chart.component';

export default angular
  .module('ratingBarChart', [])
  .component('ratingBarChart', ratingBarChartComponent).name;
